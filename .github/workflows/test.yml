# -- editorconfig-checker-disable-file
name: Test

on:
  pull_request:
  push:
    branches:
      - main
      - develop

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci
      - run: npm test
  validate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
        java-version: [11]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - uses: actions/setup-java@v2.3.1
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java-version }}
      - run: npm i -G node-w3c-validator
      - run: npm ci
      - run: npm run checkvalidate
